jenkins:
  systemMessage: "\n TESTING \n"
  numExecutors: 3
tool:
  git:
    installations:
      - name: git
        home: /usr/local/bin/git
jobs:
  - script: > 
      
  - script: >
      pipelineJob('TEST PIPELINE JOB') {
        definition {
          cps {
            sandbox(true)
            script("""\
              pipeline {
                agent any
                stages {
                  stage ('test') {
                    steps {
                      echo "hello"
                    }
                  }
                }
              }""".stripIndent())
          }
        }
      }
   
